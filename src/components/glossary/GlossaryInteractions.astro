<!-- Glossary JS lives in src/scripts/glossary-route.ts (imported from BaseLayout) so it loads on every page. -->

<style>
  :global(.glossary-in-practice) {
    border-left: 3px solid color-mix(in srgb, var(--color-brand-500, #6366f1) 70%, transparent);
    background: color-mix(in srgb, var(--color-background-secondary, #111827) 78%, transparent);
    border-radius: 0 0.5rem 0.5rem 0;
    padding: 0.75rem 1rem;
  }

  :global(.glossary-in-practice-label) {
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--color-brand-500, #6366f1);
  }

  :global(.glossary-in-practice-body) {
    color: var(--color-foreground-muted, #9ca3af);
    line-height: 1.6;
  }

  :global(.glossary-see-also-tag) {
    border: 1px solid var(--color-border, #374151);
    background: var(--color-background-secondary, #111827);
    color: var(--color-brand-500, #6366f1);
    border-radius: 999px;
    font-size: 0.75rem;
    padding: 0.2rem 0.55rem;
    transition: background-color 0.15s ease, border-color 0.15s ease;
  }

  :global(.glossary-see-also-tag:hover) {
    border-color: var(--color-brand-500, #6366f1);
    background: color-mix(in srgb, var(--color-brand-500, #6366f1) 14%, transparent);
  }

  :global(.glossary-see-also-label) {
    color: var(--color-foreground-muted, #9ca3af);
    text-transform: uppercase;
    letter-spacing: 0.04em;
    font-size: 0.72rem;
    font-weight: 700;
    margin-right: 0.25rem;
  }

  :global(.glossary-category-console-label) {
    color: var(--color-foreground-muted, #9ca3af);
    text-transform: uppercase;
    letter-spacing: 0.04em;
    font-size: 0.72rem;
    font-weight: 700;
    margin-right: 0.25rem;
  }

  :global(.glossary-category-pill) {
    white-space: nowrap;
  }

  :global([data-glossary-root] h3.glossary-term-heading) {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 0.5rem 0.75rem;
    width: 100%;
    scroll-margin-top: calc(var(--header-height, 72px) + 80px);
  }

  :global([data-glossary-root] h3[id]:not(.glossary-term-heading)) {
    scroll-margin-top: calc(var(--header-height, 72px) + 80px);
  }

  :global(.glossary-term-copy-btn) {
    display: inline-flex;
    flex-shrink: 0;
    align-items: center;
    justify-content: center;
    width: 2.25rem;
    height: 2.25rem;
    margin: 0;
    padding: 0;
    border-radius: 999px;
    border: 1px solid var(--color-border, #374151);
    background: var(--color-background-secondary, #111827);
    color: var(--color-brand-500, #6366f1);
    cursor: pointer;
    transition:
      background-color 0.15s ease,
      border-color 0.15s ease,
      color 0.15s ease;
  }

  :global(.glossary-term-copy-btn:hover) {
    border-color: var(--color-brand-500, #6366f1);
    background: color-mix(in srgb, var(--color-brand-500, #6366f1) 14%, transparent);
  }

  :global(.glossary-term-copy-btn:focus-visible) {
    outline: 2px solid var(--color-brand-500, #6366f1);
    outline-offset: 2px;
  }

  :global(.glossary-term-copy-btn svg) {
    width: 1rem;
    height: 1rem;
  }

  :global(.glossary-term-copy-icon-hidden),
  :global(.glossary-term-copy-check-hidden) {
    display: none;
  }

  :global([data-glossary-root] h2[id]) {
    scroll-margin-top: calc(var(--header-height, 72px) + 80px);
  }

  #glossary-controls {
    display: none;
  }

  @media (max-width: 768px) {
    #glossary-controls {
      position: static;
    }
  }
</style>
